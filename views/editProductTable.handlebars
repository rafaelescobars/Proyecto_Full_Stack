{{#if userType}}
  {{>adminNavbar}}
{{else}}
  {{>navbar}}
{{/if}}

<div class="container d-flex justify-content-center mt-3">
  <h1>Editar Producto Mesa {{localTable}}</h1>
  <h2 class="d-none">{{localTable}}</h2-d-none>
  <h3 class="d-none">{{productSaleId}}</h3>
</div>

<div class="container my-3 mx-0">
  <div class="row row-cols-3">
    <div class="col-md-3">

    </div>
    <div class="col-12 col-md-6">
      <form onsubmit="editProductTablePut(event)">
        <div class="mb-3">
          <label for="" class="form-label">Producto</label>
          <select class="form-select " id="productId" aria-label="Default select example" onchange="getProductPrice()" >
            {{#each products}}
              <option id="{{id}}" value="{{id}}">{{name}}</option>
            {{/each}}
          </select>
        </div>
        <div class="mb-3">
          <label for="" class="form-label">Cantidad</label>
          <input type="number" class="form-control" id="productQuantity" required />
        </div>
        <div class="mb-3">
          <label for="" class="form-label">Precio Unitario</label>
          <input type="number" class="form-control" id="productPrice" required />
        </div>
        <div class="d-flex justify-content-center">
          <button
            class="btn btn-secondary"
          >Aceptar</button>
        </div>
      </form>
    </div>

    <div class="col-md-3">
      
    </div>

  </div>

</div>

    <script src="/js/editProductTable.js"></script>
